"use client";

import React from "react";

// This component is deprecated - memory management moved to brand_manifests
export const MemoryStatusIndicator: React.FC<{ conversationId: string }> = ({ conversationId }) => {
  return null;
};
